[mypy]
# Exclude generated/third-party data files from type checking
exclude = ^data/

# Suppress missing-stub errors for third-party scientific/quantum libs
ignore_missing_imports = True

# Keep default checks; adjust in future if stricter typing desired
